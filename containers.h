#pragma once

#include "file.h"
#include "funVector.h"
#include "funDeque.h"
#include "funList.h"

void vector(char, char, char, int);
void deque(char, char, char, int);
void list(char, char, char, int);